export {
  DeviceConnectionProvider,
  DeviceConnectionProvider as WebSocketProvider,
  useDeviceConnectionContext,
  useDeviceConnectionContext as useWebSocketContext,
} from './DeviceConnectionContext';
